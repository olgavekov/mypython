password verification function: does it meet the requirements

def is_password_good(password):
    if len(password) < 8:
        return False
    else:
        checkup = False
        checklow = False
        checkdigit = False
        for p in password:
            if p.isdigit():
                checkdigit = True
            elif p.isupper():
                checkup = True
            elif p.islower():
                checklow = True
        return checkup and checklow and checkdigit
        
